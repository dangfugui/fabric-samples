# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#

version: '2'

networks:
  nuccfabric:

services:
  cli:
    container_name: cli
    image: hyperledger/fabric-tools:latest
    tty: true
    stdin_open: true
    environment:
      - SYS_CHANNEL=$SYS_CHANNEL
      - GOPATH=/opt/gopath
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock     # docker服务器的deamon地址，默认取端口的套接字：unix:///host/var/run/docker.sock
      #- FABRIC_LOGGING_SPEC=DEBUG
      - FABRIC_LOGGING_SPEC=INFO                              # 日志级别
      - CORE_PEER_ID=cli
      - CORE_PEER_ADDRESS=peer0.org1.nucc.com:7051         # 客户端连接哪个peer节点
      - CORE_PEER_LOCALMSPID=Org1MSP                          # peer节点所在的组织ID
      - CORE_PEER_TLS_ENABLED=true                            # 是否使用tls加密  如果是true 有一下设置
      # 证书文件
      - CORE_PEER_TLS_CERT_FILE=/opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/peerOrganizations/org1.nucc.com/peers/peer0.org1.nucc.com/tls/server.crt
      # 私钥文件
      - CORE_PEER_TLS_KEY_FILE=/opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/peerOrganizations/org1.nucc.com/peers/peer0.org1.nucc.com/tls/server.key
      # 根证书文件
      - CORE_PEER_TLS_ROOTCERT_FILE=/opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/peerOrganizations/org1.nucc.com/peers/peer0.org1.nucc.com/tls/ca.crt
      # 指定当前客户端身份 MSP
      - CORE_PEER_MSPCONFIGPATH=/opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/peerOrganizations/org1.nucc.com/users/Admin@org1.nucc.com/msp
    working_dir: /opt/gopath/src/github.com/hyperledger/fabric/peer
    command: /bin/bash
    volumes:
        - /var/run/:/host/var/run/
        - ./../../chaincode/:/opt/gopath/src/github.com/chaincode
        - ./../crypto-config:/opt/gopath/src/github.com/hyperledger/fabric/peer/crypto/
#        - ./../scripts:/opt/gopath/src/github.com/hyperledger/fabric/peer/scripts/
        - ./../channel-artifacts:/opt/gopath/src/github.com/hyperledger/fabric/peer/channel-artifacts
    networks:
      - nuccfabric
